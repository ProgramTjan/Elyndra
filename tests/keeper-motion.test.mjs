import assert from 'node:assert/strict';import {keeperPose} from '../dist/keeper-motion.mjs';
const base=2,visitor=[86,4,52];let rest=keeperPose(0,-1,visitor,base);assert.equal(rest[1],.65);assert.equal(keeperPose(2,3,visitor,base)[1],0);assert.equal(keeperPose(9,9,visitor,base)[1],.65);for(let t=0;t<20;t+=.1){const p=keeperPose(t,t,visitor,base);assert(p.every(Number.isFinite));assert(Math.abs(p[0])<=.065);assert(p[3]>=0&&p[3]<=.035);}assert.equal(keeperPose(2,3,visitor,base,true)[0],0);assert.equal(keeperPose(2,3,visitor,base,true)[3],0);console.log('Keeper: bounded breathing, greeting and return to sapling, reduced-motion pose.');
